services:
  mongodb:
    image: mongo:8
    container_name: ledger-sync-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: ledger_sync
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data: